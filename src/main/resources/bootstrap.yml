spring:
  application:
    name: configserver
  cloud:
    config:
      uri: http://localhost:8888/config
      server:
        prefix: /config
        bootstrap: true
        uri: https://github.com/shakuzen/scc-config-repo
